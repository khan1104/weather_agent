[project]
name = "evolusis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]>=0.120.1",
    "langchain>=1.0.2",
    "langchain-openai>=1.0.1",
    "requests>=2.32.5",
]
